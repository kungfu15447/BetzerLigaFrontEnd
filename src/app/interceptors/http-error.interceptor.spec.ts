import { HttpError.Interceptor } from './http-error.interceptor';

describe('HttpError.Interceptor', () => {
  it('should create an instance', () => {
    expect(new HttpError.Interceptor()).toBeTruthy();
  });
});
